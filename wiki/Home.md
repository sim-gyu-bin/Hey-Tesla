# Hey Tesla Wiki

잠긴 휴대폰을 주머니에서 꺼내지 않고 차량에 접근해 음성으로 프렁크 개방을 요청하는 **Android 네이티브 앱**의 제품·기술 문서다.

> 상태: 설계 및 기술 타당성 검토 단계. Android 앱 소스, Gradle 프로젝트, APK, 서버는 아직 없다. 잠금 상태 무터치 동작과 차량 제어는 실기기에서 검증하지 않았다. 이 문서는 구현 완료 보고서가 아니다.

## 핵심 결정

- Android 앱은 **Kotlin + Jetpack Compose**로 개발한다. React Native, Android 앱의 TypeScript 로직, JavaScript 브리지는 사용하지 않는다.
- 서비스·Bluetooth·오디오 수명뿐 아니라 명령 해석, 상태 머신, 설정, 게이트웨이, 앱 테스트도 Kotlin으로 통일한다.
- 첫 산출물은 실제 Tesla 제어가 없는 **Android 기술 검증 앱**이다. 초기 설정 후 잠금·주머니·차량 접근·무터치 마이크 시작을 먼저 검증한다.
- 기본 음성 비서로 선택된 `VoiceInteractionService`와 저전력 접근 신호를 조합한 경로를 우선 시험한다. 네이티브 전환이나 기본 비서 지정만으로 Android 제약이 해소된다고 가정하지 않는다.
- 기본 개발 모드는 mock/dry-run이며, 실제 명령은 프렁크 하나로 제한한다. 실차 동작은 별도 명시적 승인 후에만 시험한다.
- 서버 필요성이 확인되면 Node.js + TypeScript를 사용할 수 있다. 명령 서명은 공식 Go 기반 `vehicle-command` 구성요소를 재사용한다. 서버는 첫 검증보다 앞서 만들지 않는다.

## 문서 지도

| 문서 | 내용 |
| --- | --- |
| [제품 요구사항](Requirements.md) | 사용 시나리오, 절대 조건, MVP 범위, 안전·개인정보 원칙 |
| [네이티브 아키텍처](Native-Architecture.md) | Kotlin 구성, 서비스와 오디오 소유권, 접근 신호, 음성 처리 |
| [상태 머신과 실행 안전](State-Machine.md) | 상태 전이, 취소·만료·중복 방지, 명령 해석, 테스트 계약 |
| [기술 타당성과 공식 근거](Feasibility.md) | 확인된 사실, 구현 가설, 미확인 사항, 중단 기준 |
| [초기 설정과 실기기 검증](Device-Validation.md) | 권한, 기기 기록, 무터치 시험, 배터리·복구·오류 체크리스트 |
| [Tesla 연동과 비용](Tesla-Integration.md) | Mock, OAuth, 가상 키, 공식 서명, 차량 상태, 승인과 과금 |

## 단계별 개발 순서와 진행 조건

| 단계 | 산출물 | 다음 단계로 넘어가는 조건 | 현재 상태 |
| --- | --- | --- | --- |
| 1. 기술 타당성 | 공식 문서 근거와 검증 후보, 불확실성 목록 | 기본 비서·접근 신호·오디오 경로의 시험 항목 확정 | 문서 조사 수행, 기기 조건 미확인 |
| 2. Android 검증 앱 | 초기 설정·권한 표시, 접근 로그, 제한 음성 세션, 호출어·짧은 명령 인식, TTS, 자원 수명 로그 | 실제 차량 밖에서 잠긴 주머니 속 휴대폰에 한 번도 손대지 않고 한 문장 인식 및 종료 확인 | 미구현·미실행 |
| 3. Mock 차량 연동 | Kotlin `VehicleGateway`, 명령 정책, 취소·중복·만료·오류 처리 | 파서·상태 전이·게이트웨이 시나리오 검증, 모든 종료 경로의 오디오 해제 | 미구현·미실행 |
| 4. 실제 Tesla 연동 | 계정 승인, 가상 키, 서명 경로, 신선한 안전 상태 확인 | 수동 진단으로 인증·상태 경로 확인 후 승인받은 실차 시험, 이후 음성 결합 | 미구현·미실행 |
| 5. 배터리·안정성 | 장시간 대기·반복 접근·통화·절전·재부팅·네트워크 장애 측정 | 측정 환경·시도 횟수·실패와 복구 범위를 함께 보고 | 미실행 |

2단계에서도 안전한 음성 해석과 자원 종료는 필요하지만, 성공을 가장한 가짜 차량 제어는 넣지 않는다. UI 고도화·대규모 서버·추가 차량 명령은 핵심 무터치 검증을 통과하기 전 진행하지 않는다. 2단계부터 배터리 관찰에 필요한 이벤트를 남기되 측정 전 수치를 약속하지 않는다.

### 실패 시 처리

- 적합한 접근 신호가 없거나 마이크 시작이 공식 권한 범위에서 불가능하면, 어떤 조건이 실패했는지 기록하고 진행을 멈춘다.
- 알림 터치·매번 앱 실행·잠금 해제로 대체한 결과를 무터치 목표 달성으로 보고하지 않는다.
- 한국어 온디바이스 인식 또는 연속 발화 처리가 성립하지 않으면, 엔진 대안의 비용·라이선스·오디오 전송 조건부터 검토한다. 승인 없는 클라우드 fallback은 없다.
- 실차의 신선한 안전 상태를 확보하지 못하면 명령을 실행하지 않는다. 성공률을 높이기 위해 안전 조건을 완화하지 않는다.

## 첫 구현에서 만들 것과 만들지 않을 것

**만들 것:** 최소 Compose 진단 화면, 네이티브 서비스 생명주기, 실제 접근 이벤트 수집, 제한 오디오 세션, 호출어·온디바이스 STT 가능 여부 진단, 한국어 TTS 안내, 민감정보 없는 상태 로그.

**만들지 않을 것:** 계정 토큰을 넣은 실제 프렁크 버튼, 상시 STT, 매사용 터치가 필요한 운영 흐름, 정교한 UI, BLE 직접 차량 제어, 독자 명령 서명 구현, 차량 상태 주기 폴링 서버.

## 빌드·실행 상태

현재는 문서 저장소다. 실행 가능한 빌드·설치 명령은 아직 없다. Android 프로젝트를 생성하는 단계에서 Android Studio, JDK, Gradle/AGP, Kotlin/Compose, SDK의 호환 버전을 실제 빌드로 검증하고 고정한다. 그 뒤 재현 가능한 APK 빌드·설치 방법을 README에 추가한다. 현재 문서를 따라 APK를 만들 수 있다고 안내하지 않는다.

## 문서 운영과 게시 범위

- 루트의 `wiki/` 폴더가 Wiki 원본이다. 프로젝트 진입점은 [루트 README](../README.md)다.
- Wiki 원본은 프로젝트 저장소에서 관리한다. 커밋·푸시는 사용자 요청에 따라 수행하며, 별도 GitHub Wiki 초기화·웹 게시는 아직 하지 않았다.
- 내부 링크는 저장소에서 동작하는 상대 `.md` 링크를 사용한다. 향후 GitHub Wiki 게시가 승인되면 별도 Wiki 저장소의 페이지 경로에 맞게 링크를 변환하고 실제 게시 화면을 검증한다. 단순 복사로 동기화가 완료되었다고 간주하지 않는다.
- 사실·가설·실기기 결과를 구분하고, 새로운 실험 결과가 나오면 [기술 타당성 문서](Feasibility.md)와 [시험 기록](Device-Validation.md)을 함께 갱신한다.
- 공개 저장소이므로 VIN, Bluetooth 식별자, 계정 정보, 위치, 음성 원문, 토큰·키를 문서나 첨부 로그에 넣지 않는다.
