# 기기 검증 계획

[Wiki 홈](Home.md)

## 문서 상태와 대상

이 문서는 구현 전 검증 계획이다. 이전 검토 시점에는 앱, Gradle 프로젝트, 소스, APK가 없고 ADB 연결 기기도 0대였다. 이 기록은 당시 관찰이며 현재의 영구 상태를 단정하지 않는다. 아래 절차는 실행되지 않았으며, 표의 결과는 모두 **미실행/미확인**이다. Android/One UI/차량 펌웨어/음성 인식 엔진/모델 버전 및 실제 설정값도 실제 장비를 확인하기 전에는 미확인으로 기록한다.

초기 대상은 개인 사용자 1인·1차 조합이다.

|항목|대상|확인 상태|
|---|---|---|
|휴대전화|Galaxy S23 Ultra|실기기 설정·OS·One UI 버전 미확인|
|차량|Model Y 주니퍼 AWD Long Range|차량 펌웨어·옵션·동작 미확인|
|앱|Android 네이티브: Kotlin, Compose, Coroutines/StateFlow, DataStore|설계 단계|
|음성 경로 후보|사용자 기본 `VoiceInteractionService` + 실제 저전력 차량 접근 이벤트(CDM/CDS 등) + 필요한 구간의 microphone FGS/`AudioRecord`|설계 가설|

`CDM`만으로 microphone FGS 예외가 성립한다고 보지 않는다. 기본 비서로 지정됐다고 DSP 또는 상시 호출어 감지가 가능하다고 보지도 않는다. 플랫폼 지원 여부와 제조사 동작은 실제 기기에서 각각 검증한다. 호출어 대기 시간을 90초로 둘 경우 이는 시험값일 뿐, 배터리·지연 시간의 측정값이 아니다.

## 검증 범위와 합격 기준

합격은 모의 접근, ADB 특권, 개발용 수동 버튼 성공으로 대체할 수 없다. 실제 차량 접근과 실제 잠금 상태에서 아래 조건을 모두 관찰해야 한다.

1. 사용자가 차량에 실제로 접근할 때만 대기 경로가 시작 또는 재개된다.
2. 초기 설정 완료 후 휴대전화가 잠긴 채 주머니에 있는 상태에서 접근하고, 해당 사용 과정 중 앱 실행·알림 터치·잠금 해제 등 화면 조작이 한 번도 없었는지 검증한다.
3. 호출어 뒤 한 문장 명령을 인식하고, 명령 처리 후 오디오가 종료된다.
4. 접근 이탈, 취소, 오류에서는 오디오 캡처·STT·세션 자원이 해제된다. 저전력 접근 대기 서비스의 상주는 별도 설계·검증 대상으로 허용할 수 있으나, 마이크를 점유해서는 안 된다.
5. 차량 제어는 [Tesla 통합 전제](Tesla-Integration.md)의 승인·안전 조건을 충족한 경우만 별도 검증한다.

검증의 대상 상태는 `DISABLED`, `WAITING_FOR_VEHICLE`, `WAKE_WORD_LISTENING`, `COMMAND_LISTENING`, `CONFIRMING`, `VALIDATING`, `EXECUTING`, `COOLDOWN`, `ERROR`다. `UNKNOWN`은 상태 머신 상태가 아니라 차량 명령 결과가 불명인 별도 결과다. 세부 전이는 [상태 머신](State-Machine.md), 플랫폼 구조는 [네이티브 아키텍처](Native-Architecture.md)를 따른다.

## 초기 설정과 사용자 승인

초기 설정과 최초 앱 열기는 사용자가 수행·승인할 수 있다. 다만 정상 운영에서 매 사용마다 앱을 열거나 화면 조작을 요구하는 경로는 제안하지 않는다. 구현되지 않은 기능의 설정이 가능하다고 말하지 않으며, 실제 구현된 API·Android 버전에서 요구하는 항목만 다음 순서로 진행한다.

1. 구현 후 대상 휴대전화에서 OS·One UI 버전과 관련 설정 화면을 기록한다.
2. mock/dry-run을 선택하고, 실차 명령 미실행·권한·기본 비서 변경 영향을 고지한다.
3. 실제 선택 API가 요구하는 최소 런타임 권한만 요청한다.
4. `VoiceInteractionService` 경로를 구현한 경우에만 사용자가 기본 비서 선택을 승인하며, 기존 비서 영향과 해제 방법을 확인한다.
5. 구현한 접근 감지 API로 목표 차량 association을 만들고 실제 신호 수신을 확인한다.
6. 온디바이스 `ko-KR` 인식 엔진·모델, 외부 입력 지원, 네트워크 없이 인식 가능한지를 확인한다.
7. 초기 설정 완료 후에만 잠금·주머니·무터치 실기기 시험을 시작한다.

|항목|검토·요청 시점|조건과 제한|결과|
|---|---|---|---|
|`RECORD_AUDIO` 런타임 권한|음성 수집 전|마이크를 실제 사용하는 구현에 한정|미실행/미확인|
|foreground service 선언·type|구현·설치 시|`microphone` type 및 해당 권한·manifest 요구를 실제 API 버전에서 확인; 런타임 `RECORD_AUDIO`와 구분|미실행/미확인|
|Nearby Bluetooth scan/connect|선택한 접근 감지 API가 요구할 때|스캔·연결 목적과 Android 버전을 먼저 확인|미실행/미확인|
|CDM association·background permission|접근 이벤트에 CDM을 채택할 때|CDM background permission 필요 여부와 조건을 실제 API 버전에서 확인|미실행/미확인|
|기본 비서 지정|`VoiceInteractionService` 경로를 시험할 때|기존 비서 교체 영향과 해제 방법을 먼저 고지|미실행/미확인|
|`POST_NOTIFICATIONS`|선택한 Android API와 사용자 노출 설계가 요구할 때|거부가 언제나 FGS 시작 불가를 뜻하지는 않으나, FGS 사용자 표시와 제한을 기기에서 시험|미실행/미확인|
|위치 권한|스캔 목적별 검토 후 필요한 경우만|무조건 추가하지 않는다. `neverForLocation` 선언·필터링이 실제 근접도 추론과 부합하는지 검토|미실행/미확인|
|Samsung Never sleeping|사용자가 선택할 수 있는 초기 설정|프로세스 생존 정책일 뿐 마이크 제한 우회 수단이 아님|미실행/미확인|

재부팅 후 첫 잠금 해제 전후, 일반 프로세스 종료, 최근 앱 목록 제거, 사용자 강제 중지는 서로 다른 조건으로 기록한다. 특히 강제 중지 후 사용자가 손대지 않아도 자동 복구된다고 보장하지 않는다.

## 실기기 검증 절차

각 케이스는 차량·휴대전화의 실제 상태, 권한·기본 비서 설정, 네트워크 상태, 소리 출력 경로를 결과 기록에 남긴다. 호출어와 명령은 연속 발화와 분리 발화를 모두 시험한다. 바람·옷 마찰 등 소음, 통화 및 다른 앱의 마이크 경합(무음 경합 포함), TTS 루프, 블루투스·스피커·유선 등 오디오 라우팅도 별도 관찰한다.

|검증 항목|수행 절차|기대 관찰|결과|
|---|---|---|---|
|실제 접근·잠금·주머니|실차에서 멀어진 뒤 휴대전화를 잠그고 주머니에 넣은 채 접근|실제 접근 이벤트만 대기 조건에 반영; 앱 수동 실행 없음|미실행/미확인|
|한 번도 터치하지 않은 음성|설정 후 앱을 열지 않고 실제 접근 뒤 호출어와 한 문장 명령 발화|허용된 경로에서만 인식하고 종료 후 오디오 해제|미실행/미확인|
|연속 발화|“헤이 테슬라 프렁크 열어줘”를 쉬거나 다시 말하지 않고 발화|명령 첫 음절 손실 없이 같은 세션에서 최종 명령 인식; 실패는 실패로 기록|미실행/미확인|
|소음 내성|바람·옷 마찰 환경에서 같은 발화 반복|오인식·미인식·상태 종료를 구분 기록|미실행/미확인|
|마이크 경합|통화 중, 타 앱 녹음 중, 무음으로 점유된 경우 시험|오디오 입력 공유 정책에 따라 안전하게 거절·중단; 강제 점유 없음|미실행/미확인|
|TTS·라우팅|TTS 응답과 각 출력 경로에서 재시험|자기 음성 재인식 루프가 없고 입력·출력 종료가 명확|미실행/미확인|
|원거리 장기 대기|차량에서 떨어진 상태로 장시간 방치|불필요한 음성 수집·반복 깨우기 없이 절전|미실행/미확인|
|접근 이탈|대기 중 차량에서 멀어짐|대기와 오디오가 종료되고 상태가 안전하게 전이|미실행/미확인|
|BT 재연결 debounce·cooldown|짧은 시간에 연결·해제를 반복|중복 시작·명령 없이 debounce 및 cooldown 적용|미실행/미확인|
|화면 꺼짐·절전|잠금·화면 꺼짐·절전 조건을 각각 적용|플랫폼 제한과 실제 생존 상태를 사실대로 기록|미실행/미확인|
|인터넷 단절·차량 수면|명령 전후 네트워크를 끊고 차량 수면 조건 관찰|실행으로 가장하지 않고 실패·대기·불명 결과를 구분|미실행/미확인|
|앱 업데이트|정상 대기 상태에서 업데이트 후 재시작 조건 확인|권한·association·기본 비서 상태의 유지/재설정을 기록|미실행/미확인|
|취소·부정·회상·모호 명령|각 발화와 애매한 참조를 입력|명확화·취소·안전한 거절을 구분|미실행/미확인|
|중복·만료·늦은 결과|같은 명령 반복, TTL 경과, 지연 응답을 주입 가능한 환경에서 재현|중복 전송·만료 실행·불명 결과 재전송이 없음|미실행/미확인|
|재부팅·첫 잠금 해제 전|재부팅 후 사용자가 첫 잠금 해제하기 전 실제 접근 시험|플랫폼이 허용한 범위의 상태만 관찰; 자동 복구 보장 없음|미실행/미확인|
|재부팅·첫 잠금 해제 후|첫 잠금 해제 뒤 설정을 변경하지 않고 실제 접근 시험|초기 설정·대기 상태의 실제 복원 여부를 기록|미실행/미확인|
|일반 프로세스 종료|시스템이 일반 프로세스를 종료하도록 유도한 뒤 접근 시험|서비스·association별 실제 복원 또는 중단을 구분|미실행/미확인|
|최근 앱 목록 제거|최근 앱 목록에서 앱을 제거한 뒤 접근 시험|제거와 강제 중지를 혼동하지 않고 실제 동작 기록|미실행/미확인|
|사용자 강제 중지|설정 화면에서 사용자가 강제 중지한 뒤 접근 시험|무터치 자동 복구를 보장하지 않으며 재초기화 필요 여부 기록|미실행/미확인|
|온디바이스 한국어 미지원·모델 미설치|네트워크 없이 `ko-KR` 인식 요청|클라우드 fallback 없이 명시적 실패; 캡처·세션 자원 해제|미실행/미확인|
|통화·녹음 종료 후 재획득|마이크 경합을 끝낸 뒤 세션 만료와 cooldown을 기다린 후 재시도|만료·cooldown을 지킨 새 세션만 입력을 재획득|미실행/미확인|

## 배터리 측정·진단과 개인정보

배터리 비교는 충전선을 분리한 상태에서 한다. 같은 화면 상태, 통신 조건, 관찰 시간을 맞춘 앱 비활성 기준선과 앱 원거리 대기를 비교한다. 원거리 대기의 합격 불변식은 마이크·STT 활성 시간이 0이라는 것이며, 이는 아직 측정값이 아니다. 각 실행에서 시작·종료 배터리, 기간, 세션 활성 시간, 스캔 이벤트, FGS, wake lock 자료를 읽기 전용 진단으로 기록한다. USB 충전은 배터리 측정을 왜곡하고 시스템 배터리 퍼센트는 해상도가 낮다. 종료 후 연결해서 누적 자료를 수집하거나, 측정 영향을 기록한 진단 경로를 사용한다. 환경·반복 횟수·성공·실패 횟수를 함께 보고하며 단일 실행을 일반화하거나 미측정 수치를 주장하지 않는다.

배터리·서비스 상태 진단은 읽기 전용 명령을 예시로만 사용한다. 예: `adb shell dumpsys batterystats`, `adb shell dumpsys activity services`, `adb shell dumpsys media.audio_flinger`. 아직 구현물이 없으므로 현재 실행할 빌드·설치·검증 명령은 없다. `batterystats --reset`, `pm grant`, 백그라운드 제한 우회 또는 권한 우회는 사용하지 않는다.

원시 시스템 로그에는 개인식별정보, 차량 식별번호, 토큰이 포함될 수 있으므로 공유 전 마스킹한다. 오디오는 기본적으로 저장하거나 전송하지 않는다. 앱 이벤트 로그는 `sessionId`, `state`, `event`, `elapsed`, `success`, `reason`과 필요한 비민감 측정값으로 제한한다. `reason`에는 정해진 오류 코드만 사용하고 원본 예외·응답을 그대로 넣지 않는다. 발화 원문·VIN·토큰·계정 식별자는 기록하지 않는다.

## 빌드 전제

현재 Gradle 파일, 소스, APK가 없으므로 실행할 빌드·설치 명령도 없다. 구현을 시작할 때 Android Studio, JDK, Android SDK, Gradle wrapper의 호환 조합을 실제 환경에서 확인한 뒤 고정한다. SDK와 라이브러리 버전도 구현 시 검증 후 고정하며, 이 문서는 특정 버전을 약속하지 않는다.

## 결과 기록 템플릿

```text
일시:
검증자:
휴대전화 / Android / One UI:
차량 / 차량 펌웨어:
음성 인식 엔진·모델:
권한·기본 비서·association 설정:
네트워크 / 차량 수면 / 잠금·주머니 상태:
검증 항목:
수행 절차:
기대 관찰:
실제 관찰:
결과: 통과 / 실패 / 보류 / 미실행
상태 전이 및 이벤트 로그(민감정보 마스킹):
재현 조건 및 다음 조치:
시도 수:
성공 / 실패 수:
접근→준비 지연:
인식 지연:
오디오 캡처 활성 시간:
STT 활성 시간:
배터리 시작 / 종료 / 관찰 기간:
세션 활성 시간 / 스캔 이벤트 / FGS / wakelock:
```

## 공식 근거

- Android foreground service 백그라운드 시작 제한: <https://developer.android.com/develop/background-work/services/fgs/restrictions-bg-start>
- `VoiceInteractionService`: <https://developer.android.com/reference/android/service/voice/VoiceInteractionService>
- `SpeechRecognizer` 및 오프라인 선호 힌트: <https://developer.android.com/reference/android/speech/SpeechRecognizer>, <https://developer.android.com/reference/android/speech/RecognizerIntent#EXTRA_PREFER_OFFLINE>
- Android 오디오 입력 공유: <https://developer.android.com/media/platform/sharing-audio-input>
- Android 15 stopped state: <https://developer.android.com/about/versions/15/behavior-changes-all#stopped-state>
- Samsung 앱 관리: <https://developer.samsung.com/mobile/app-management.html>

관련 문서: [요구사항](Requirements.md), [네이티브 아키텍처](Native-Architecture.md), [상태 머신](State-Machine.md), [Tesla 통합 전제](Tesla-Integration.md).
